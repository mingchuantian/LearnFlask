from flask_bootstrap import Bootstrap
#The extensions are usually imported from a flask_<name> package, where <name> is the extension name. 
bootstrap-Bootstrap(app)   #the extension is initialized by passing the application instance as an argument in the constructor. 


